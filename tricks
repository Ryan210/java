In eclips 
using alt + / can make wise tiping

import java.util.Arrays;

Arrays.lenth不需要括号

hello.print(a);
//字符串需要“”

由于数组中存放的是基本类型int的数据，而基本类型在传递时都是值传递，因此这种foreach的写法其实是把数组中每个元素遍历后分别赋给了变量i，因此为变量i赋值并不会改变数组中的每个元素的值，在处理基本类型时建议使用以下方式，直接设置数组中每个元素的值：
for (int i = 0; i < nums.length; i++) {
nums[i]=1;
}
